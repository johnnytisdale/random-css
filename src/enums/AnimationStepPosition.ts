enum AnimationStepPosition {
  END = "end",
  JUMP_BOTH = "jump-both",
  JUMP_END = "jump-end",
  JUMP_NONE = "jump-none",
  JUMP_START = "jump-start",
  START = "start",
}

export default AnimationStepPosition;
